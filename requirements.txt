fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0
aiosqlite>=0.20
asyncpg>=0.29
pydantic[email]>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
python-multipart>=0.0.9
structlog>=24.0
pytest>=8.0
httpx>=0.27
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.0
jinja2>=3.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# ---------- 资料导入可选依赖 ----------
pandas>=2.0
openpyxl>=3.1
# pdfplumber>=0.11  # PDF 解析（可选，安装后启用）
# pytesseract>=0.3  # 图片 OCR（可选，需安装 tesseract-ocr 系统包）
# python-docx>=0.8  # Word 文档解析（可选）
# pillow>=10.0      # 图片处理（OCR 依赖）
