# 🔌 4 个开发者插件安装详细指南

**目标插件:**
1. 🐙 GitHub
2. 🍎 Build macOS Apps
3. 🟢 NVIDIA
4. 🛠️ Codex App Tools

**预计时间:** 15-20 分钟
**前提条件:** 已修复 `no_biscuit_no_service` 认证错误

---

## ⚠️ 重要提示

### 认证状态检查

在安装插件之前,**必须先确保 Codex 应用已正确登录**。

如果你之前遇到了 `no_biscuit_no_service` 错误,请先执行:

```powershell
# 1. 关闭 Codex 应用
# 2. 删除无效的认证文件
Remove-Item "D:\CodexSystem\.codex\auth.json" -Force

# 3. 重新打开 Codex 应用
# 4. 使用 OpenAI 账号登录
```

**验证登录成功:**
- 应用右上角显示你的头像
- 设置 → 账号 显示你的邮箱

---

## 🐙 1. GitHub 插件安装

### 📋 插件信息
- **版本:** 0.1.6
- **认证:** ✅ 需要 GitHub OAuth
- **位置:** `D:\CodexSystem\.codex\plugins\cache\openai-api-curated\github\11c74d6b\`
- **配置文件:**
  - `.app.json` - App Connector 配置
  - `.mcp.json` - MCP 服务器配置
  - `README.md` - 安装说明

### 🎯 功能
- 查看和管理 GitHub 仓库
- 审查 Pull Requests
- 处理 Issues
- 调试 GitHub Actions CI/CD
- 准备代码变更发布

---

### 📝 安装步骤

#### 方式 1: ChatGPT 登录用户 (推荐)

**适用条件:** 你的 Codex 使用 ChatGPT/OpenAI 账号登录

**步骤:**

1. **打开 Codex 应用**

2. **进入插件管理**
   ```
   Codex 应用 → 左下角 ⚙️ 设置 → Plugins
   ```

3. **找到 GitHub 插件**
   - 在插件列表中搜索 "GitHub"
   - 应该看到 GitHub 图标和描述

4. **点击 Install 或 Enable**
   - 会弹出 GitHub OAuth 授权页面

5. **授权 GitHub**
   - 如果未登录 GitHub,先登录
   - 点击 "Authorize OpenAI" 或 "Authorize Codex"
   - 授权以下权限:
     - ✅ 读取代码仓库
     - ✅ 读写 Issues 和 Pull Requests
     - ✅ 读取 GitHub Actions

6. **完成授权**
   - 授权成功后会自动跳转回 Codex
   - GitHub 插件状态变为 "Enabled" 或 "已启用"

7. **验证安装**
   在 Codex 对话中输入:
   ```
   "列出我的 GitHub 仓库"
   ```
   如果成功,会显示你的仓库列表

---

#### 方式 2: API Key 用户 (高级)

**适用条件:** 你的 Codex 使用 API Key 登录

**步骤:**

1. **创建 GitHub Personal Access Token (PAT)**

   a. 访问 https://github.com/settings/tokens
   
   b. 点击 "Generate new token (classic)"
   
   c. 设置 Token 信息:
      - Note (名称): `Codex Plugin`
      - Expiration (过期): 90 days 或 No expiration
   
   d. 选择权限 (Scopes):
      ```
      ✅ repo (完整仓库访问)
        ✅ repo:status
        ✅ repo_deployment
        ✅ public_repo
        ✅ repo:invite
        ✅ security_events
      ✅ workflow (GitHub Actions)
      ✅ read:org (组织信息)
      ✅ read:user (用户信息)
      ✅ user:email (邮箱)
      ```
   
   e. 点击 "Generate token"
   
   f. **复制生成的 token** (格式: `ghp_xxxxxxxxxxxx`)
      ⚠️ 这是唯一一次可以看到完整 token!

2. **配置环境变量**

   **方式 A: 用户级环境变量 (推荐)**
   ```powershell
   # 设置环境变量 (永久)
   [System.Environment]::SetEnvironmentVariable('GITHUB_PAT_TOKEN', 'ghp_你的token', 'User')
   
   # 验证设置
   [System.Environment]::GetEnvironmentVariable('GITHUB_PAT_TOKEN', 'User')
   ```

   **方式 B: 系统级环境变量**
   ```
   1. Win + R → 输入 sysdm.cpl → 回车
   2. 高级 → 环境变量
   3. 用户变量 → 新建
   4. 变量名: GITHUB_PAT_TOKEN
   5. 变量值: ghp_你的token
   6. 确定
   ```

3. **重启 Codex 应用**
   - 完全关闭 Codex
   - 重新打开以加载新的环境变量

4. **启用插件**
   - 设置 → Plugins → GitHub → Enable

5. **验证配置**
   在 Codex 中测试:
   ```
   "使用 GitHub 插件列出我的仓库"
   ```

---

### 🧪 测试 GitHub 插件

安装成功后,尝试以下命令:

```bash
# 基础功能
"列出我的 GitHub 仓库"
"查看 LiuHao-AI-OS 仓库的详细信息"

# Pull Requests
"列出我的待审查 PR"
"查看 PR #123 的改动"

# Issues
"列出所有未关闭的 issues"
"创建一个新 issue: 修复登录 bug"

# CI/CD
"为什么最近的 CI 失败了?"
"查看最新的 GitHub Actions 运行记录"
```

---

### 🛠️ 故障排查

#### 问题 1: 授权页面无法打开
**解决方案:**
- 检查网络连接
- 允许浏览器弹窗
- 尝试使用系统默认浏览器

#### 问题 2: "no_biscuit_no_service" 错误
**解决方案:**
- 删除 `D:\CodexSystem\.codex\auth.json`
- 重新登录 Codex

#### 问题 3: PAT Token 无效
**解决方案:**
- 检查 token 格式 (应该是 `ghp_` 开头)
- 验证 token 权限
- 重新生成 token

#### 问题 4: 无法访问私有仓库
**解决方案:**
- 确认已授权 `repo` 权限
- 重新授权插件

---

## 🍎 2. Build macOS Apps 插件安装

### 📋 插件信息
- **版本:** 0.1.4
- **认证:** ✅ 需要 Apple OAuth
- **位置:** `D:\CodexSystem\.codex\plugins\cache\openai-api-curated\build-macos-apps\11c74d6b\`
- **平台:** ⚠️ 主要用于 macOS 开发

### 🎯 功能
- 构建 macOS 应用程序
- SwiftUI 和 AppKit 指导
- 调试和性能分析
- Xcode 项目管理
- macOS 特定功能实现

---

### 📝 安装步骤

**⚠️ 注意:** 你当前使用的是 Windows 系统,这个插件主要针对 macOS 开发。

**如果你需要 macOS 开发:**

1. **打开插件管理**
   ```
   Codex 应用 → 设置 → Plugins
   ```

2. **找到 Build macOS Apps 插件**
   - 搜索 "macOS" 或 "Build macOS Apps"

3. **点击 Install**
   - 会要求 Apple ID 授权
   - 或者要求 Apple Developer 账号

4. **完成授权**
   - 按照提示登录 Apple ID
   - 授权必要的权限

5. **验证安装**
   ```
   "帮我创建一个 SwiftUI macOS 应用"
   ```

---

### 💡 Windows 用户替代方案

**如果你主要在 Windows 开发,建议:**

❌ 跳过此插件
✅ 使用其他更适合 Windows 的插件:
- GitHub (代码管理)
- Browser (Web 开发)
- Visualize (数据可视化)
- Computer Use (Windows 自动化)

---

## 🟢 3. NVIDIA 插件安装

### 📋 插件信息
- **版本:** 1.0.2
- **认证:** ✅ 需要 NVIDIA OAuth
- **位置:** `D:\CodexSystem\.codex\plugins\cache\openai-api-curated\nvidia\11c74d6b\`
- **作者:** NVIDIA

### 🎯 功能
- NVIDIA AI 工作流指导
- GPU 加速计算 (CUDA, cuDNN)
- 深度学习框架 (TensorRT, Triton)
- 机器人仿真 (Isaac)
- 3D 渲染和图形 (Omniverse)
- NVIDIA SDK 和工具

---

### 📝 安装步骤

1. **打开插件管理**
   ```
   Codex 应用 → 设置 → Plugins
   ```

2. **找到 NVIDIA 插件**
   - 搜索 "NVIDIA"
   - 应该看到绿色 NVIDIA 图标

3. **点击 Install**
   - 会弹出 NVIDIA 账号授权页面

4. **登录 NVIDIA 账号**
   - 如果没有账号,需要先注册: https://www.nvidia.com/
   - 使用 NVIDIA 账号登录

5. **授权 Codex**
   - 点击 "Authorize" 或 "授权"
   - 同意访问你的 NVIDIA 开发者资源

6. **完成安装**
   - 授权成功后自动返回 Codex
   - 插件状态变为 "Enabled"

---

### 🧪 测试 NVIDIA 插件

```bash
# GPU 相关
"我的 NVIDIA GPU 型号是 RTX 4090,如何优化深度学习训练?"
"如何安装 CUDA 和 cuDNN?"

# AI/ML 框架
"帮我配置 TensorRT 加速推理"
"如何使用 NVIDIA Triton 部署模型?"

# 3D 和仿真
"如何在 Omniverse 中创建场景?"
"使用 Isaac Sim 做机器人仿真"
```

---

### 💡 适用场景

**适合安装 NVIDIA 插件的用户:**
- ✅ 拥有 NVIDIA GPU
- ✅ 进行深度学习训练
- ✅ 需要 GPU 加速计算
- ✅ 开发 AI/ML 应用
- ✅ 进行 3D 渲染或仿真

**不需要安装的情况:**
- ❌ 没有 NVIDIA GPU
- ❌ 不做 AI/ML 开发
- ❌ 主要做 Web 开发

---

## 🛠️ 4. Codex App Tools 插件安装

### 📋 插件信息
- **版本:** 0.1.0
- **认证:** ❌ 无需 OAuth (官方捆绑)
- **位置:** `D:\CodexSystem\.codex\plugins\cache\openai-bundled\codex-app-tools\0.1.0\`
- **作者:** OpenAI
- **类型:** MCP 服务器

### 🎯 功能
- Codex 桌面应用内部工具集
- MCP (Model Context Protocol) 服务器
- 应用功能扩展
- 线程管理
- 自动化支持

---

### 📝 安装步骤

**好消息:** 这个插件是官方捆绑的,无需 OAuth 授权!

1. **打开插件管理**
   ```
   Codex 应用 → 设置 → Plugins
   ```

2. **找到 Codex App Tools**
   - 可能显示为 "App Tools" 或 "Codex Tools"

3. **点击 Enable**
   - 无需授权,直接启用

4. **等待 MCP 服务器启动**
   - 插件会启动内部 MCP 服务器
   - 通常需要 5-10 秒

5. **验证状态**
   - 插件状态应该显示为 "Enabled" 或 "运行中"

---

### 🔍 检查 MCP 服务器状态

运行以下命令检查:

```powershell
# 查看 MCP 配置
Get-Content "D:\CodexSystem\.codex\plugins\cache\openai-bundled\codex-app-tools\0.1.0\.mcp.json" | ConvertFrom-Json | ConvertTo-Json -Depth 3
```

**预期输出:**
```json
{
  "mcpServers": {
    "codex_app": {
      "enabled": true,  // 应该是 true
      "command": "cmd.exe",
      ...
    }
  }
}
```

---

### 🧪 测试 Codex App Tools

这个插件主要是底层工具,不像其他插件有直观的功能。

它支持的功能包括:
- 线程创建和管理
- 自动化任务
- 应用内部通信

**间接测试方法:**
```
"创建一个新的 task"
"列出所有任务"
```

如果这些命令能正常工作,说明 Codex App Tools 已成功运行。

---

## 📊 安装优先级和时间估算

| 插件 | 优先级 | 时间 | 难度 | 推荐 |
|------|--------|------|------|------|
| **Codex App Tools** | 🔥 高 | 1 分钟 | ⭐ 简单 | ✅ 立即安装 |
| **GitHub** | 🔥 高 | 5 分钟 | ⭐⭐ 中等 | ✅ 强烈推荐 |
| **NVIDIA** | ⭐ 可选 | 3 分钟 | ⭐⭐ 中等 | 🤔 按需安装 |
| **Build macOS Apps** | ⚠️ 低 | 5 分钟 | ⭐⭐ 中等 | ❌ Windows 用户不需要 |

---

## 🚀 推荐安装顺序

### 第 1 步: Codex App Tools (1 分钟)
- 最简单,无需授权
- 是其他功能的基础

### 第 2 步: GitHub (5 分钟)
- 编码最重要的插件
- 代码管理必备

### 第 3 步: NVIDIA (可选,3 分钟)
- 仅当你有 NVIDIA GPU 且做 AI/ML 开发时安装

### 第 4 步: Build macOS Apps (跳过)
- Windows 用户不需要

---

## ✅ 安装验证清单

完成安装后,检查以下内容:

### Codex App Tools
- [ ] 插件状态显示 "Enabled"
- [ ] MCP 服务器 `enabled: true`
- [ ] 可以创建新 task

### GitHub
- [ ] 插件状态显示 "Enabled"
- [ ] 可以列出仓库
- [ ] 可以查看 PRs 和 Issues

### NVIDIA (如果安装)
- [ ] 插件状态显示 "Enabled"
- [ ] 可以询问 GPU 相关问题
- [ ] 获得 NVIDIA 工具指导

---

## 🆘 遇到问题?

### 通用解决方案

1. **认证问题**
   ```powershell
   Remove-Item "D:\CodexSystem\.codex\auth.json" -Force
   # 重新登录 Codex
   ```

2. **插件无法加载**
   - 重启 Codex 应用
   - 检查网络连接
   - 清除缓存

3. **OAuth 授权失败**
   - 使用系统默认浏览器
   - 清除浏览器 cookies
   - 检查防火墙设置

---

### 在 Codex 中寻求帮助

```
"我的 GitHub 插件无法授权"
"Codex App Tools 启动失败"
"如何验证插件是否正常工作?"
```

---

## 📝 快速命令参考

### 删除无效认证
```powershell
Remove-Item "D:\CodexSystem\.codex\auth.json" -Force
```

### 检查 MCP 状态
```powershell
Get-Content "D:\CodexSystem\.codex\plugins\cache\openai-bundled\codex-app-tools\0.1.0\.mcp.json" | ConvertFrom-Json | Select-Object -ExpandProperty mcpServers | Select-Object -ExpandProperty codex_app | Select-Object enabled
```

### 设置 GitHub PAT
```powershell
[System.Environment]::SetEnvironmentVariable('GITHUB_PAT_TOKEN', 'ghp_你的token', 'User')
```

### 验证环境变量
```powershell
[System.Environment]::GetEnvironmentVariable('GITHUB_PAT_TOKEN', 'User')
```

---

## 🎉 总结

### 推荐安装组合 (Windows 开发者)

**立即安装:**
1. ✅ Codex App Tools (1 分钟)
2. ✅ GitHub (5 分钟)

**按需安装:**
3. 🤔 NVIDIA (如果你有 NVIDIA GPU 且做 AI 开发)

**不推荐:**
4. ❌ Build macOS Apps (仅 macOS 开发需要)

**总计时间:** 6-9 分钟

---

**🚀 现在就开始安装吧!**

从 Codex App Tools 开始,它最简单。然后安装 GitHub,这是最重要的开发者工具!

完成后告诉我结果,我可以帮你验证是否成功! 💪

---

*指南创建时间: 2026-08-24*
*适用系统: Windows*
*Codex 版本: 26.818.x*
