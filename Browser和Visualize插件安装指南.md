# 🌐📊 Browser 和 Visualize 插件安装指南

## ✅ 插件状态检查

### Browser 插件
- **版本:** 26.818.31338
- **状态:** ✅ 已缓存,可直接启用
- **位置:** `D:\CodexSystem\.codex\plugins\cache\openai-bundled\browser\`
- **类型:** OpenAI 官方捆绑插件 (无需 OAuth)

### Visualize 插件
- **版本:** 1.0.22
- **状态:** ✅ 已缓存,可直接启用
- **位置:** `D:\CodexSystem\.codex\plugins\cache\openai-bundled\visualize\`
- **类型:** OpenAI 官方捆绑插件 (无需 OAuth)

---

## 🚀 快速启用 (3 种方法)

### 方法 1: Codex 应用界面 (推荐)

1. **打开插件管理**
   ```
   Codex 应用 → ⚙️ 设置 → Plugins (插件)
   ```

2. **启用 Browser 插件**
   - 在列表中找到 "Browser" 或 "浏览器"
   - 点击 "Install" 或 "Enable" 按钮
   - ✅ 无需授权,立即启用

3. **启用 Visualize 插件**
   - 在列表中找到 "Visualize" 或 "可视化"
   - 点击 "Install" 或 "Enable" 按钮
   - ✅ 无需授权,立即启用

---

### 方法 2: 对话式安装 (最简单)

直接在 Codex 对话中说:
```
"启用 Browser 插件"
"Enable Visualize plugin"
```

或者当你提出相关需求时,Codex 会自动提示安装:
```
"打开 localhost:3000"  → 会提示安装 Browser
"把这个数据做成图表"    → 会提示安装 Visualize
```

---

### 方法 3: 自动触发安装

这两个插件会在你需要时自动提示安装:

**Browser 自动触发场景:**
- 当你说 "打开 localhost:xxxx"
- 当你说 "测试我的网页"
- 当你说 "截图当前页面"

**Visualize 自动触发场景:**
- 当你说 "可视化这个数据"
- 当你说 "创建一个图表"
- 当你说 "绘制架构图"

---

## 🌐 Browser 插件详解

### 核心功能

#### 1. 本地开发测试
```
✅ "用 Browser 打开 http://localhost:3000"
✅ "打开 localhost:8080"
✅ "测试我的前端应用"
```

#### 2. 页面交互
```
✅ "点击登录按钮"
✅ "在用户名框中输入 'admin'"
✅ "滚动到页面底部"
✅ "填写这个表单"
```

#### 3. 页面检查
```
✅ "截图当前页面"
✅ "检查这个元素的 CSS"
✅ "找到所有的按钮"
✅ "这个页面的标题是什么?"
```

#### 4. 调试和测试
```
✅ "检查控制台有没有错误"
✅ "测试响应式布局"
✅ "验证登录流程"
✅ "检查页面加载时间"
```

### 使用场景

**前端开发:**
- 🎨 实时预览页面修改
- 🔍 检查元素样式
- 🐛 调试 JavaScript 错误
- 📱 测试移动端视图

**自动化测试:**
- ✅ 测试用户流程
- ✅ 验证表单提交
- ✅ 检查链接是否有效
- ✅ 截图对比测试

**网页抓取:**
- 📄 提取页面内容
- 🖼️ 下载页面资源
- 🔗 获取所有链接
- 📊 分析页面结构

### 支持的 URL 格式
```
✅ http://localhost:3000
✅ http://127.0.0.1:8080
✅ http://[::1]:5000
✅ file:///C:/path/to/file.html
✅ 当前应用内浏览器标签
```

### 别名和调用方式
```
@browser
@browser-use
browser-use
Browser
in-app browser
```

---

## 📊 Visualize 插件详解

### 核心功能

#### 1. 数据可视化
```
✅ "将这个 CSV 数据转换成图表"
✅ "创建一个销售趋势折线图"
✅ "把这个 JSON 数据可视化"
✅ "生成一个交互式数据浏览器"
```

#### 2. 图表类型
- 📈 折线图 (Line Charts)
- 📊 柱状图 (Bar Charts)
- 🥧 饼图 (Pie Charts)
- 📉 散点图 (Scatter Plots)
- 🗺️ 地图 (Maps)
- 📐 热力图 (Heatmaps)
- 🌳 树状图 (Tree Diagrams)
- 🔗 关系图 (Network Graphs)

#### 3. 架构和图表
```
✅ "绘制系统架构图"
✅ "创建数据库 ER 图"
✅ "显示组件依赖关系"
✅ "画一个流程图"
```

#### 4. 3D 可视化
```
✅ "创建一个 3D 数据模型"
✅ "显示三维散点图"
✅ "构建交互式 3D 场景"
```

#### 5. UI 预览
```
✅ "预览这个组件设计"
✅ "展示 UI 原型"
✅ "创建交互式原型"
```

### 使用场景

**数据分析:**
- 📊 探索数据集
- 🔍 发现数据模式
- 📈 追踪趋势变化
- 🎯 识别异常值

**技术文档:**
- 🏗️ 系统架构图
- 🗄️ 数据库设计图
- 🔄 流程图和时序图
- 📦 组件关系图

**报告和演示:**
- 📑 交互式报告
- 🎨 可视化仪表板
- 📊 数据故事讲述
- 🖼️ 动态演示

**原型设计:**
- 🎨 UI/UX 概念验证
- 🖱️ 交互式原型
- 📱 应用界面预览

### 交互特性
```
✅ 过滤数据
✅ 选择细节
✅ 比较场景
✅ 调整参数
✅ 实时更新
✅ 导出图表
```

---

## 🎯 实战示例

### Browser 插件实战

#### 示例 1: 测试本地 Web 应用
```
你: "我刚改了前端代码,帮我测试一下"
Codex: "好的,我用 Browser 打开 localhost:3000"
      → 自动打开应用内浏览器
      → 导航到你的本地服务器
      → 截图展示当前状态
      
你: "点击导航栏的'关于'链接"
Codex: → 模拟点击操作
      → 展示新页面内容
```

#### 示例 2: 调试页面问题
```
你: "为什么登录按钮点不了?"
Codex: → 打开开发者工具
      → 检查按钮元素
      → 查看控制台错误
      → 分析 CSS 样式
      "发现按钮被 z-index 较高的遮罩层覆盖了"
```

#### 示例 3: 自动化测试
```
你: "测试整个注册流程"
Codex: → 打开注册页面
      → 填写表单字段
      → 提交表单
      → 验证成功消息
      → 截图记录每一步
      "注册流程测试通过 ✅"
```

---

### Visualize 插件实战

#### 示例 1: 数据可视化
```
你: "我有一个 sales.csv 文件,帮我可视化"
Codex: → 读取 CSV 数据
      → 分析数据结构
      → 创建交互式图表
      → 添加过滤器和控制器
      "这是你的销售数据仪表板 📊"
```

#### 示例 2: 系统架构图
```
你: "画一个我的微服务架构图"
Codex: → 分析项目结构
      → 识别服务依赖
      → 创建可交互的架构图
      → 标注通信协议
      "这是你的系统架构可视化 🏗️"
```

#### 示例 3: 数据探索
```
你: "帮我探索这个数据集的模式"
Codex: → 创建多维度图表
      → 添加趋势分析
      → 突出显示异常值
      → 提供筛选工具
      "发现了 3 个有趣的数据模式 🔍"
```

---

## 🔧 配置和自定义

### Browser 插件配置

**支持的浏览器:**
- ✅ Codex 内置浏览器 (默认)
- ✅ Chrome
- ✅ Edge
- ✅ Brave
- ✅ Opera
- ✅ Vivaldi

**Skills 位置:**
```
D:\CodexSystem\.codex\plugins\cache\openai-bundled\browser\26.818.31338\skills\
```

**文档:**
- 📄 API 使用规范
- 📄 可访问性指南
- 📄 浏览器安全策略
- 📄 故障排查指南

---

### Visualize 插件配置

**Skills 位置:**
```
D:\CodexSystem\.codex\plugins\cache\openai-bundled\visualize\1.0.22\skills\
```

**资源文件:**
- `visualize.html` - 可视化容器模板
- `visualize.css` - 样式表
- `render.py` - 渲染脚本

**支持的数据格式:**
- ✅ CSV
- ✅ JSON
- ✅ Excel
- ✅ SQL 查询结果
- ✅ 纯文本数据

---

## ✅ 验证插件是否启用

### 测试 Browser 插件
在 Codex 中输入:
```
"用 Browser 打开 https://example.com"
```

如果插件正常工作,会:
1. 打开应用内浏览器
2. 导航到指定网址
3. 显示页面内容或截图

---

### 测试 Visualize 插件
在 Codex 中输入:
```
"用 Visualize 创建一个简单的柱状图,数据: A=10, B=20, C=15"
```

如果插件正常工作,会:
1. 创建交互式图表
2. 显示在对话中
3. 提供交互控制

---

## 🛠️ 故障排查

### Browser 插件问题

#### 问题 1: 无法打开浏览器
**解决方案:**
1. 确认插件已启用
2. 检查 URL 格式是否正确
3. 确认本地服务器正在运行
4. 重启 Codex 应用

#### 问题 2: 页面加载失败
**解决方案:**
1. 检查网络连接
2. 验证端口号是否正确
3. 查看防火墙设置
4. 检查服务器日志

#### 问题 3: 无法交互
**解决方案:**
1. 等待页面完全加载
2. 确认元素存在且可见
3. 检查 JavaScript 错误
4. 尝试刷新页面

---

### Visualize 插件问题

#### 问题 1: 图表无法显示
**解决方案:**
1. 确认插件已启用
2. 检查数据格式是否正确
3. 验证数据不为空
4. 刷新 Codex 界面

#### 问题 2: 交互功能失效
**解决方案:**
1. 重新生成图表
2. 检查浏览器控制台
3. 清除缓存
4. 更新 Codex 应用

#### 问题 3: 性能问题
**解决方案:**
1. 减少数据点数量
2. 简化图表复杂度
3. 分批次可视化
4. 使用数据采样

---

## 📊 性能和限制

### Browser 插件
- ⚠️ 主要用于本地开发和测试
- ⚠️ 某些网站可能阻止自动化访问
- ⚠️ 复杂页面可能加载较慢
- ✅ 对本地 localhost 最优化

### Visualize 插件
- ⚠️ 大数据集可能需要时间渲染
- ⚠️ 复杂 3D 场景对性能有要求
- ✅ 支持增量加载
- ✅ 可导出为静态文件

---

## 🎓 最佳实践

### Browser 插件最佳实践

1. **明确指定 URL**
   ```
   ✅ "打开 http://localhost:3000"
   ❌ "打开我的应用"
   ```

2. **等待页面加载**
   ```
   ✅ "等页面加载完成后点击按钮"
   ❌ "立即点击按钮"
   ```

3. **使用具体的选择器**
   ```
   ✅ "点击 id 为 'submit-btn' 的按钮"
   ❌ "点击那个按钮"
   ```

---

### Visualize 插件最佳实践

1. **提供清晰的数据**
   ```
   ✅ 结构化的 CSV/JSON
   ❌ 非结构化文本
   ```

2. **指定图表类型**
   ```
   ✅ "用折线图显示趋势"
   ❌ "可视化这个"
   ```

3. **说明交互需求**
   ```
   ✅ "创建可筛选的图表"
   ❌ "做个图表"
   ```

---

## 🔗 组合使用技巧

### Browser + Visualize 组合

**场景 1: 数据抓取 + 可视化**
```
你: "从 localhost:3000/api/stats 获取数据并可视化"
Codex: → Browser 打开 API 端点
      → 提取 JSON 数据
      → Visualize 创建图表
      "这是你的 API 数据仪表板 📊"
```

**场景 2: 测试结果可视化**
```
你: "测试我的图表组件,然后分析性能"
Codex: → Browser 打开组件页面
      → 收集性能指标
      → Visualize 展示性能数据
      "渲染时间: 45ms, 内存: 2.3MB 📈"
```

---

## ✅ 安装后检查清单

安装完成后,确认以下功能可用:

### Browser 插件
- [ ] 可以打开 localhost URL
- [ ] 可以截图
- [ ] 可以点击元素
- [ ] 可以提取页面内容

### Visualize 插件
- [ ] 可以创建基础图表
- [ ] 可以可视化 CSV 数据
- [ ] 可以交互操作
- [ ] 可以绘制架构图

---

## 📚 相关资源

### Browser 插件文档
```
D:\CodexSystem\.codex\plugins\cache\openai-bundled\browser\26.818.31338\docs\
```
- API 使用规范
- 浏览器安全策略
- 可访问性指南
- 故障排查

### Visualize 插件文档
```
D:\CodexSystem\.codex\plugins\cache\openai-bundled\visualize\1.0.22\skills\
```
- SKILL.md - 使用说明
- 示例代码
- 模板文件

---

## 🎉 下一步

1. [ ] 在 Codex 应用中启用 Browser 插件
2. [ ] 在 Codex 应用中启用 Visualize 插件
3. [ ] 测试 Browser: `"打开 localhost:3000"`
4. [ ] 测试 Visualize: `"创建一个示例图表"`
5. [ ] 探索更多功能!

---

**🚀 现在就去 Codex 应用中启用这两个插件吧!**

它们都是官方捆绑插件,无需额外授权,点击即可启用!

---

*最后更新: 2026-08-24*
*Browser 版本: 26.818.31338*
*Visualize 版本: 1.0.22*
