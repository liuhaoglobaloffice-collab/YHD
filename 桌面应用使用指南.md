# 🖥️ 鎏灏 AI-OS 桌面应用 - 使用指南

## ✅ 已创建桌面启动方案

现在你可以像使用普通桌面软件一样启动鎏灏 AI-OS！

---

## 🚀 快速开始

### 方法 1: 创建桌面图标（推荐）

**步骤**:
1. 双击运行: `D:\LiuHao-AI-OS\创建桌面快捷方式.vbs`
2. 看到"创建成功"提示
3. 桌面上会出现"鎏灏AI-OS"图标
4. 以后双击桌面图标即可启动

---

### 方法 2: 直接运行启动脚本

**简单版（命令行）**:
```
双击: D:\LiuHao-AI-OS\启动鎏灏AI.bat
```

**GUI版（图形界面）**:
```
右键 → 打开方式 → PowerShell
文件: D:\LiuHao-AI-OS\启动器GUI.ps1
```

---

## 📁 文件说明

### 1. 启动鎏灏AI.bat
**功能**:
- ✅ 自动启动后端服务（端口 8000）
- ✅ 自动启动前端服务（端口 3000）
- ✅ 自动打开浏览器
- ✅ 显示账号密码提示

**使用**:
- 双击文件即可运行
- 后台启动服务，前台打开应用
- 关闭命令窗口停止服务

### 2. 创建桌面快捷方式.vbs
**功能**:
- 在桌面创建快捷图标
- 图标名称：鎏灏AI-OS
- 双击图标启动系统

**使用**:
- 只需运行一次
- 以后用桌面图标启动

### 3. 启动器GUI.ps1
**功能**:
- 图形界面启动器
- 进度条显示
- 状态提示
- 一键启动

**使用**:
- 右键 → 使用 PowerShell 运行
- 或在 PowerShell 中执行: `.\启动器GUI.ps1`

---

## 🎯 推荐使用流程

### 首次设置
```
1. 运行: 创建桌面快捷方式.vbs
2. 点击"确定"
3. 桌面出现图标
```

### 日常使用
```
1. 双击桌面上的"鎏灏AI-OS"图标
2. 等待 5-10 秒启动
3. 浏览器自动打开应用
4. 输入账号: 1163661699
5. 输入密码: yhd2579..lq
6. 开始使用！
```

### 关闭系统
```
方法 1: 直接关闭浏览器（服务保持运行）
方法 2: 关闭启动时的命令窗口（完全停止）
方法 3: 任务管理器结束 node/python 进程
```

---

## 💡 使用技巧

### 技巧 1: 开机自启动
1. 按 `Win + R`
2. 输入 `shell:startup`
3. 将"鎏灏AI-OS"快捷方式复制到打开的文件夹
4. 下次开机自动启动

### 技巧 2: 固定到任务栏
1. 右键桌面快捷方式
2. 选择"固定到任务栏"
3. 任务栏出现图标，随时点击启动

### 技巧 3: 快捷键启动
1. 右键桌面快捷方式 → 属性
2. 在"快捷键"栏输入: `Ctrl + Alt + L`
3. 点击"确定"
4. 按 `Ctrl + Alt + L` 即可启动

### 技巧 4: 静默启动
如果不想看到命令窗口，可以：
1. 使用 PowerShell GUI 版本
2. 或修改 .bat 文件，添加 `@echo off` 和隐藏窗口参数

---

## 🔧 常见问题

### Q1: 双击图标没反应？
**原因**: 可能被安全软件拦截
**解决**: 
- 右键 → 以管理员身份运行
- 或添加到杀毒软件白名单

### Q2: 端口被占用？
**症状**: 提示端口 3000 或 8000 已被使用
**解决**:
```powershell
# 查找占用端口的进程
netstat -ano | findstr :8000
netstat -ano | findstr :3000

# 结束进程（替换 PID）
taskkill /F /PID <进程ID>
```

### Q3: 启动失败？
**检查**:
1. Python 是否已安装
2. Node.js 是否已安装
3. 项目路径是否正确
4. 依赖是否已安装

**修复**:
```bash
# 重新安装依赖
cd D:\LiuHao-AI-OS
pip install -r requirements.txt

cd frontend
npm install
```

### Q4: 浏览器没有自动打开？
**手动打开**:
```
http://localhost:3000
```

### Q5: 想修改启动端口？
编辑 `启动鎏灏AI.bat`，修改端口号：
```batch
--port 8000  改成  --port 你的端口
```

---

## 📊 启动流程

```
双击桌面图标
    ↓
运行 启动鎏灏AI.bat
    ↓
[1/4] 检查环境
    ↓
[2/4] 启动后端 (3秒)
    ↓
[3/4] 启动前端 (5秒)
    ↓
[4/4] 打开浏览器
    ↓
显示登录页面
    ↓
输入账号密码
    ↓
✅ 进入 CEO 桌面
```

**总耗时**: 约 10-15 秒

---

## 🎨 自定义图标

如果想更换桌面图标：

1. 准备一个 `.ico` 图标文件
2. 右键快捷方式 → 属性
3. 点击"更改图标"
4. 选择你的图标文件
5. 点击"确定"

**推荐图标资源**:
- https://icons8.com
- https://www.flaticon.com
- 搜索关键词: AI, Technology, Dashboard

---

## 🚀 进阶方案: 打包成独立 EXE

如果你想要一个完全独立的 `.exe` 文件（双击运行，不需要 Python/Node）：

### 方案: Electron 打包

**步骤**:
1. 安装 Electron
```bash
cd D:\LiuHao-AI-OS\frontend
npm install --save-dev electron electron-builder
```

2. 创建 Electron 主文件
```javascript
// electron/main.js
const { app, BrowserWindow } = require('electron');

function createWindow() {
  const win = new BrowserWindow({
    width: 1400,
    height: 900,
    icon: 'icon.ico'
  });
  win.loadURL('http://localhost:3000');
}

app.whenReady().then(createWindow);
```

3. 打包
```bash
npm run electron:build
```

**优点**:
- 完全独立的 .exe 文件
- 无需 Python/Node 环境
- 可以发布给其他人使用
- 专业桌面应用体验

**缺点**:
- 打包文件较大（100-200MB）
- 需要额外配置
- 需要约 15-30 分钟

---

## 📝 总结

### ✅ 当前方案（立即可用）

| 方式 | 优点 | 耗时 |
|------|------|------|
| 桌面快捷方式 | 简单快速 | 1分钟 |
| BAT脚本 | 一键启动 | 已完成 |
| PowerShell GUI | 图形界面 | 已完成 |

### 🚀 推荐工作流

```
1. 首次: 运行"创建桌面快捷方式.vbs"
2. 日常: 双击桌面"鎏灏AI-OS"图标
3. 登录: 1163661699 / yhd2579..lq
4. 开始工作！
```

---

## 📞 文件位置

```
D:\LiuHao-AI-OS\
├── 启动鎏灏AI.bat              (命令行启动器)
├── 创建桌面快捷方式.vbs         (创建桌面图标)
├── 启动器GUI.ps1               (图形界面启动器)
└── 桌面应用使用指南.md          (本文件)
```

---

**创建时间**: 2026-08-23  
**版本**: Desktop Launcher v1.0  
**状态**: ✅ 已完成可用

---

🎉 **现在就去创建你的桌面快捷方式吧！**

**立即体验**:
1. 双击: `D:\LiuHao-AI-OS\创建桌面快捷方式.vbs`
2. 双击桌面上的"鎏灏AI-OS"图标
3. 等待启动
4. 开始使用！
