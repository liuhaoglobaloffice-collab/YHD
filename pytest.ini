[pytest]
# Ensure pytest discovers tests under tests/ and recognizes both common patterns
testpaths = tests
python_files = test_*.py *_test.py
# Warnings are not elevated to errors here to avoid collection failures caused by deprecation warnings
# adjust filterwarnings in CI if you want strict failure on specific warnings
